# Issue #2 — Due dates

**Status:** Closed
**Opened by:** tom.w

Can we have due dates?

It would be nice.

Would be useful for work stuff.

---

**dev team:** Tom, can you give us more detail? What format? Should overdue
tasks warn or sort differently? Is this a flag on `add` or a separate command?

**tom.w:** idk just like a date field. iso format maybe

**dev team (closed):** Implemented as `-d / --due YYYY-MM-DD` on `taskr add`.
Overdue highlighting not added — open a new issue if that's needed.
